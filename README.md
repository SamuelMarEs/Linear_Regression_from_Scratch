# Linear Regression Model from Scratch
The project consists in creating a Multiple Linear Regression class using only numpy. 
The class contains both a `fit` and a `predict` method. For the `fit` method, both the **Normal equation** and **Gradient descent** are implemented separately. Coefficients are then compared to *sklearn*'s linear model.
---
